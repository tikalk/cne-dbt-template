version: 2

sources:
  - name: entities_data
    description: >
          Collection data saved in json format
    database: "{{ env_var('SOURCE_DATABASE', env_var('BIGQUERY_DATABASE')) }}"
    schema: BRONZE  

groups:
  - name: entities
    owner:
      email: entities@nagomisecurity.com
      slack: ask-engine
      github: tikal-dbt
