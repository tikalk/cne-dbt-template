version: 2

macros:
  - name: generate_schema_name
    description: Decide on schema name based on environment
    arguments:
      - name: custom_schema_name
        type: string
        description: Name of schema to use
      - name: node
        type: string
        description: Name of the node
